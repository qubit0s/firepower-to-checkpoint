# Python dependencies (installed into .venv by setup.sh)
# Install manually with:  pip install -r requirements.txt
ciscoconfparse2>=0.7.74
PyYAML>=6.0
# ansible-dev-tools bundles ansible (incl. ansible-core), ansible-lint, yamllint,
# ansible-navigator and more -- one install covers running and linting the playbooks.
ansible-dev-tools
